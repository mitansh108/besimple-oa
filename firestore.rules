rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to all documents (adjust for production)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
